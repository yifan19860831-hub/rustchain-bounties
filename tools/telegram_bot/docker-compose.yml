version: '3.8'

services:
  rustchain-bot:
    build: .
    container_name: rustchain-telegram-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN:-YOUR_BOT_TOKEN_HERE}
    networks:
      - rustchain-network

networks:
  rustchain-network:
    driver: bridge
